getfeedbackEdgeSet(vertex, ed) #função que seta os vertices e as arestas
# iniciar as arestas
set os vertices vertex
set as arestas ed

#Passar como parametro os vertices e as arestas
getfeedbackEdgeSet() # Chamada da função

return getfeedbackEdgeSet(vertex, ed) 
# Definir a função que seta os vertices e arestas

define getdeedbackEdgeSet(vertex,ed):
# loop para ser executado até ed

for ed in ed:
    #setar todas os pesos das arestas


#fazer o cadastro no IEL
#precisa ser pelo Itau (corrente) - Enviar novamente os dados da conta
Perguntar a ufal o que falta pra Ufal
#Fazer a carteira de trabalho digital

Até quando o atestado